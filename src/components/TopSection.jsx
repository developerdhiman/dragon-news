import logo from '../assets/logo.png';

const TopSection = () => {
    return (
        <div>
            <img src={logo} alt="" />
            <p>Journalism Without Fear or Favor</p>
            
        </div>
    );
};

export default TopSection;